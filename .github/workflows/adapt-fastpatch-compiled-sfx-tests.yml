name: Adapt FastPatch compiled SFX tests

on:
  push:
    branches:
      - security/fastpatch-immutable-staging

permissions:
  contents: write

jobs:
  adapt:
    if: github.event.head_commit.message == 'Trigger FastPatch final adapter'
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: security/fastpatch-immutable-staging
          persist-credentials: true
      - name: Apply permanent FastPatch finalization
        shell: pwsh
        run: |
          python .github/scripts/_temp_fastpatch_test_init.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python .github/scripts/_temp_fastpatch_adapt_compiled_test.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python .github/scripts/_temp_fastpatch_finalize.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python .github/scripts/_temp_fastpatch_exit_propagation.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python .github/scripts/_temp_fastpatch_raw_process_tests.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Parse and run secure-staging regression
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          foreach ($path in @(
            'scripts/Install-ProtonVPNPatch.ps1',
            'scripts/Install-ProtonVPNCompletePatch.ps1',
            'scripts/New-ProtonVPNPatchSfx.ps1',
            '.github/scripts/test-fast-patch-secure-staging.ps1'
          )) {
            $tokens = $null
            $errors = $null
            [void][Management.Automation.Language.Parser]::ParseFile($path,[ref]$tokens,[ref]$errors)
            if ($errors.Count) { throw "$path : $($errors[0].Message)" }
          }
          & powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .github/scripts/test-fast-patch-secure-staging.ps1
          if ($LASTEXITCODE -ne 0) { throw "Secure-staging regression failed with exit code $LASTEXITCODE." }
      - name: Commit permanent hardening and regression
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          git rm --force .github/scripts/_temp_fastpatch_adapt_compiled_test.py
          git rm --force .github/scripts/_temp_fastpatch_test_init.py
          git rm --force .github/scripts/_temp_fastpatch_finalize.py
          git rm --force .github/scripts/_temp_fastpatch_exit_propagation.py
          git rm --force .github/scripts/_temp_fastpatch_raw_process_tests.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add scripts/Install-ProtonVPNPatch.ps1 scripts/Install-ProtonVPNCompletePatch.ps1 .github/scripts/test-fast-patch-secure-staging.ps1
          git commit -m 'Finalize immutable FastPatch staging and rollback hardening'
          git push origin HEAD:security/fastpatch-immutable-staging
