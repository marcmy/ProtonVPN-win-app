# v5.1.8 future-port conflict resolutions

This record compares the fork change with the official v5.1.8 implementation. It is
specific to the reconciliation based on `v5.1.8` (`d2a4f8bc`) and fork head
`bfb114625`. Validation remains in progress.

## Validation snapshot (2026-09-24)

- GitHub Actions run [35874131124](https://github.com/marcmy/ProtonVPN-win-app/actions/runs/35874131124) ran against fork head `bfb114625`. Patch-tooling regression checks passed; the port then stopped at the intended fail-closed gate because cleaned fork changes still conflicted with v5.1.8. No automatic conflict resolution or artifact build followed.
- The exact v5.1.8-equivalence cleanup was merged in PR [#109](https://github.com/marcmy/ProtonVPN-win-app/pull/109) at `843e2474`. Fresh run [35968032162](https://github.com/marcmy/ProtonVPN-win-app/actions/runs/35968032162) used that head and the official `v5.1.8` tag; the cleanup gate passed, identifying 22 known historical backports plus the exact feedback-animation commit. The merge then correctly failed closed on 28 content/add-add/modify-delete conflicts, beginning with `ProtonVPN.Networking.sln`. No build ran. These paths are covered by the feature-by-feature ledger below; no unresolved conflict was auto-selected.
- Fresh candidate run [35973538795](https://github.com/marcmy/ProtonVPN-win-app/actions/runs/35973538795) passed cleanup, merge, restore, client/launcher/service builds, and dependency staging, then exposed two lost fork behaviors in the connection regression project: Guest Hole disconnect serialization and the per-app port-forwarding preference. The target branch was not pushed. The release behavior was compared against the current fork and adapted below; the complete affected connection test project now passes locally (67/67), including the retained/added regressions. Full CI rerun is pending.
- Focused local checks passed: connection-manager regressions (12/12); VPN endpoint/scanner/module/loopback regressions (12/12); credential and certificate mapper tests (12/12); service connection-probe/global-exception tests (10/10); Smart Routing presentation tests (4/4); global-search cancellation (1/1); launcher build (0 warnings, 0 errors).
- The full `ProtonVPN.Client` build now passes under the pinned .NET 8 SDK (15 warnings, 0 errors). This exercises all client XAML pages, including the repaired feedback view. The complete `ProtonVPN.Client.Common.UI.Tests` project passes (37/37), the focused connection-feedback integration tests pass (7/7), and the client exception-handler tests pass (3/3).
- The full patch-tooling regression suite passes with the exact v5.1.8-equivalence cleanup and same-file replay case. This was verified in a clean synthetic Git fixture and again in the fresh GitHub Actions run before the semantic conflict gate.
- The feedback integration-test project now compiles against the v5.1.8 dispatcher contract. `UserSettingsMigrationTest` also passes (3/3). Full-solution and end-to-end validation remain outstanding.
- NuGet restore completed from cached packages; vulnerability-audit lookup emitted `NU1900` because `api.nuget.org` was unreachable. Full solution and end-to-end validation remain outstanding.
- Per user direction, the client exception policy is resolved to official v5.1.8 behavior: log the WinUI exception and mark it handled.

## Resolved

| File | Fork intent and v5.1.8 comparison | Resolution and coverage |
| --- | --- | --- |
| `src/Client/Common/ProtonVPN.Client.Common.UI/Exceptions/ClientGlobalExceptionHandler.cs` | Official v5.1.8 commit `13cdf29ea` logs WinUI unhandled exceptions and marks them handled to suppress a client crash. The fork intentionally withheld `Handled = true` pending independent validation; its logging architecture does not require that policy. | Per user direction, take the official v5.1.8 handler behavior, including its event-args alias and base initialization call; retain the fork's structured `TryLogException` path. The focused client handler suite passes (3/3), including a regression asserting that the exception is logged and marked handled. |
| `ProtonVPN.Networking.sln` | Fork commit `47e8d42ce` removed the stale `ProtonVPN.IPFilter` project from this legacy solution. Official commit `3b4745a7a` deletes the whole solution as part of the dependency-cache transition, and no remaining solution/project/build-script references were found. | Keep the v5.1.8 deletion; the narrower fork cleanup is obsolete in this release architecture. Validate the release build/CI; no standalone behavior test applies. |
| `src/Client/ProtonVPN.Client/App.xaml.cs` | Fork commit `1558dbe66` wires the structured client exception handler. v5.1.8 already constructs the handler in a field initializer and calls `Initialize(this)` in the constructor; the fork only moves construction into the constructor. | Keep the v5.1.8 field initializer and its initialization call; behavior is equivalent and no handler wiring is lost. The full client build passes, and client exception-handler regressions pass (3/3). |
| `src/Client/ProtonVPN.Client/Handlers/ProfileConnectAndGoHandler.cs` | Fork commit `ac6ed86c3` removes `async void` from the receiver and schedules a `Task` helper with `FireAndForget`. v5.1.8 already uses that same safe structure, one-second delay, and duplicate-profile guard. The only remaining difference is equivalent disconnected/connected branching. | Keep the v5.1.8 implementation and early return. Retain the focused handler tests if present; test discovery/validation is pending. |
| `src/Client/ProtonVPN.Client/Models/Connections/HostLocationItemBase.cs` | Fork commits `8d7256a39` and `04d43a315` add measured-ping filtering, cancellation, and explicit refresh propagation without long-lived per-row subscriptions. v5.1.8 commit `0dba863b3` changes Smart Routing to describe all visible virtual-server host countries rather than one server. | Combine the fork filter/refresh lifecycle with the v5.1.8 multi-server description; omit the now-unused `_lastKnownServers` field. Retain filter-session tests and add a presentation regression for the multi-host description; broader validation is pending. |
| `src/Client/ProtonVPN.Client/Models/Connections/ServerLocationItemBase.cs` | Fork health work adds `IServerHealthSource`, a direct service probe, and health endpoint selection. v5.1.8 adds `SmartRoutingLabel` through its localization extension and binds it in the connection-row UI. | Keep both feature sets and both namespaces; retain the v5.1.8 `SmartRoutingLabel` property alongside fork probe behavior. Smart Routing presentation tests passed (4/4); health-probe validation is pending. |
| `src/Client/ProtonVPN.Client/UI/Main/Home/Card/ConnectionCardComponentView.xaml` | Fork commit `036b761ef` reserves a separate column for the Smart Routing tag. v5.1.8 adds the host-country tooltip and adjusts subtitle alignment/margins; its tag otherwise shares column 2 with Tor. | Keep four columns and Smart Routing in column 3, while retaining v5.1.8 subtitle layout and tooltip. The separate-column and multi-host presentation regressions passed in the Smart Routing suite (4/4). |
| `src/Client/ProtonVPN.Client/UI/Main/Sidebar/Search/SearchResultsPageViewModel.cs` | Fork commits add Browse All, exclusion filtering, stale-result generations, cancellation, and ping-threshold results; v5.1.8 still exposes the older non-cancellable `IGlobalSearch` API. | Keep the fork search flow and its cancellation-aware `IGlobalSearch` contract/implementation, while using `ReloadResultsAsync` consistently and preserving both example-country and example-city notifications. The added cancelled-token regression passed (1/1); broader view-model integration validation is pending. |
| `src/Client/ProtonVPN.Client/UI/Main/Sidebar/SidebarComponentViewModel.cs` | Fork commits `28725c225`, `95574ec29`, and `c3c852a82` make nonempty typing navigate to Search, preserve browse-all while focus changes, and handle clearing an already-empty query. v5.1.8 only forwards query text. | Keep the fork interaction flow; it is required for Browse All and keeps search results visible when typing from another child page. No focused sidebar-view-model test exists in this tree; the full client build passes, while dedicated UI interaction validation remains outstanding. |
| `src/Client/Settings/ProtonVPN.Client.Settings.Contracts/IUserSettings.cs` and `src/Client/Settings/ProtonVPN.Client.Settings/UserSettings.cs` | The fork preserves the legacy `DnsBlockMode` value so users without a stored `IsLocalDnsEnabled` value can migrate it, and adds the per-app port-forwarding setting. It also exposes old window width/position properties. v5.1.8 replaces those window members with `WindowLocation` and a global-settings migration hook that reads the legacy keys. | Keep `IsPortForwardingForAppsEnabled` and the DNS migration/property; do not reintroduce the superseded window members. Added coverage for both legacy DNS modes and for preserving an already-stored boolean. `UserSettingsMigrationTest` now passes (3/3); port-setting and window-location persistence coverage remains separate. |
| `src/Client/Settings/ProtonVPN.Client.Settings/GlobalSettings.cs` | Fork commits `2b2cf5e66` and `d87a38371` add skipped-update and per-app port-forwarding persistence. v5.1.8 adds `WindowLocation` with a migration hook from legacy user settings. | Keep both fork cache-backed settings and the v5.1.8 window-location migration. Existing skipped-update integration tests remain; port-setting and window migration test execution is pending. |
| `src/Client/Settings/ProtonVPN.Client.Settings.Contracts/Messages/FeatureFlagChange.cs` | Both trees carry old/new boolean and payload values plus `HasChanged`; the conflict is formatting only. | Retain the shared payload-aware value type. It backs the auto-dismiss timeout regression tests. |
| `src/Client/Settings/ProtonVPN.Client.Settings/Observers/FeatureFlagsObserver.cs` | Fork commit `acd98a625` adds notification when the connection-feedback timeout payload changes. Its older `IsConnectionFeedbackEnabled` property name does not match v5.1.8's `IFeatureFlagsObserver.ConnectionFeedback` contract. | Keep the release `FeatureFlag ConnectionFeedback` API and emit payload-only changes under that exact property name, filling `OldPayload`/`NewPayload` only for this flag. The existing observer test asserts payload-only notification and naming. |
| `src/Client/ProtonVPN.Client/UI/Main/Home/Card/Feedback/ConnectionFeedbackComponentViewModel.cs` | v5.1.8 adjusts timer/reinitialization behavior; fork commits `c4877ba2d` and `8e72e6803` prevent stale delayed dismiss/submit completions from mutating a later connection session. | Preserve release visibility/timer behavior and payload-driven timeout, with the fork's per-session guards and reset behavior. Existing integration regressions cover payload changes and disconnect/reconnect during submit or dismissal. |
| `src/Client/ProtonVPN.Client/UI/Main/Home/Card/Feedback/ConnectionFeedbackComponentView.xaml` | Official v5.1.8 commit `96775c9d3` already contains the `DismissingFeedback` animation. Later fork commit `a2cb79dcd57a538f198dcfb0a2a2e217b5dfbe79` independently adds that same state; it is not labeled `Port`/`Backport`, so historical backport cleanup correctly leaves it alone, but a normal merge duplicates the named XAML state. | Keep the single official state and its immediate animation, while retaining the fork's per-session dismissal behavior in the view model. Add an exact-SHA cleanup mapping scoped to target v5.1.8; its regression proves the release state remains and later fork edits to the same file replay. The full client build passed under .NET 8 (15 warnings, 0 errors); related UI and feedback integration suites passed (37/37 and 7/7). |
| `src/Tests/ProtonVPN.Integration.Tests/UI/Main/Home/Card/Feedback/ConnectionFeedbackComponentViewModelTest.cs` | The v5.1.8 `IUIThreadDispatcher` contract includes `TryEnqueue(Func<Task>, ...)`; the combined test fake had omitted it, although the source-fork fake implements it. | Restore the source-fork fake's synchronous callback implementation against the release interface. The connection-feedback integration tests pass (7/7), including disconnect/reconnect during dismissal and submit. |
| `src/Client/Common/ProtonVPN.Client.Common.UI.Tests/ProtonVPN.Client.Common.UI.Tests.csproj` | The new tests exercise a WinUI library, so they need the same WinUI test-host and runtime-configuration settings as the existing integration-test project. Without those settings the adapter mixed incompatible WinUI/standard test-host assets and could not start. | Configure the WinUI test host, runtime configuration, and local dependency copying consistently with the integration project. The complete UI test suite passes (37/37), including the exception-handler regressions (3/3). |
| `src/ProtonVPN.Service/Vpn/ConnectionProbe.cs` | The conflict is limited to duplicate validation-warning call sites: the fork inlines the same warning, while v5.1.8 centralizes it in `LogValidationError`. | Keep the v5.1.8 helper and both server-validation checks; this preserves the validation behavior and a consistent quoted log format. `ConnectionProbeTest` and global exception-handler tests passed as part of the service suite (10/10). |
| `src/ProtonVPN.Vpn/Config/Module.cs` | The fork's split-route self-healing change makes `SplitTunnelRouting` disposable. Its history narrows Autofac registration to `ISplitTunnelRouting`; v5.1.8 registers every implemented interface. | Keep the explicit `ISplitTunnelRouting` registration so disposal stays owned by the routing component/container lifecycle instead of exposing `IDisposable` as an application service. The new registration regression and VPN focused suite passed (12/12). |
| `src/ProtonVPN.Vpn/PortScanning/TcpPortScanner.cs`, `src/ProtonVPN.Vpn/OpenVpn/OpenVpnHandshake.cs`, and `src/ProtonVPN.Vpn/PortScanning/IUdpPingClient.cs` | Fork commit `6300db0ed` makes its OpenVPN-handshake TCP probe cancellable. Official v5.1.8 commit `7929bcae1` removes that handshake and uses a cancellable TCP connect; this scanner serves OpenVPN, WireGuard, and ProTun TCP/TLS endpoints, so protocol-neutral reachability is the correct shared contract. The UDP interface is identical apart from conflict markers/newline. | Keep the v5.1.8 connect-only implementation with cancellation, remove the unused handshake type, and retain the shared UDP interface. The loopback regression proving a reachable TCP endpoint needs no OpenVPN response and the VPN focused suite passed (12/12). |
| `src/Logging/ProtonVPN.Logging.Contracts/Events/AppServiceLogs/AppServiceCrashLog.cs` and `src/ProtonVPN.Service/Start/ServiceGlobalExceptionHandler.cs` | Both implementations are identical apart from the fork's stale 2025 copyright header versus v5.1.8's 2026 header. | Keep the v5.1.8 headers and identical crash/error log event mapping. Global exception-handler/service regressions passed as part of the service suite (10/10). |
| `src/Logging/ProtonVPN.Logging.Events/GlobalExceptionHandlerBase.cs` | The clean source snapshot omits this historical backport file, while the current `marc/proton` tree and the deferred fork commits depend on it. The release and fork both normalize non-Exception throws, flatten aggregate diagnostics, write event/file logs, and invoke the fatal callback; fork changes mainly extract normalization and tighten nullable annotations. | Keep the v5.1.8 base implementation, which has equivalent runtime behavior and matches the service/client handler contract. Service-side global exception-handler tests pass (10/10), and client exception-handler tests pass (3/3). |
| `src/ProtonVPN.Service/Start/Bootstrapper.cs` | The fork's current `marc/proton` version still uses the exception-bearing `Action<Exception>` callback and isolates each fatal cleanup action so one failure cannot skip later cleanup. The clean snapshot contains an intermediate `EventHandler` signature that does not match `GlobalExceptionHandlerBase.OnFatalException`. | Use the exception-bearing callback, retain the release's per-action best-effort cleanup, and keep the fork's non-null resolution constraints. The service project compiled in the focused service test run (10/10); no focused Bootstrapper cleanup test was found. |
| `src/Client/Logic/Connection/ProtonVPN.Client.Logic.Connection.Tests/ConnectionManagerTest.cs` | The target tests status/error/intent-change notifications. Fork commits `5896e7adf` and `68dc34ae7` add Guest Hole disconnect ordering and stale-active-marker recovery regressions. | Keep both sets; they cover independent connection-state contracts and exercise the current Guest Hole behavior without replacing the release's status-notification tests. The combined connection-manager suite passes (12/12). |
| `src/Client/Logic/Connection/ProtonVPN.Client.Logic.Connection/ConnectionManager.cs` | v5.1.8 only flips the Guest Hole active flag. The current fork detects an active-to-inactive transition and replays a cached Pinging/Connected state, preventing a normal connection that raced the Guest Hole status message from remaining hidden. | Restore the guarded replay with `FireAndForget`; the merged regression failed before this fix and passed after it. The combined connection-manager suite passed (12/12). |
| `src/Client/Logic/Connection/ProtonVPN.Client.Logic.Connection/GuestHole/GuestHoleManager.cs` and `GuestHoleManagerTest.cs` | The release retains `ConnectionStatusChangedMessage`, but `VpnStateIpcEntityHandler` suppresses raw `Disconnected` with `NoneKeepEnabledKillSwitch` during Guest Hole transitions. The fork's recent changes observe raw service disconnects, ignore the pre-connect tunnel teardown, and keep the semaphore held until a requested disconnect completes. The first candidate lost that lifecycle behavior. | Keep the release status-message flow and adapt the fork's bounded, synchronized disconnect gate and guarded `VpnStateIpcEntity` receiver to it. Preserve regressions for disconnect ordering; add coverage proving startup teardown is ignored and requested disconnect completes from the raw service state. The full connection test project passes locally (67/67); CI rerun is pending. |
| `src/Client/Logic/Connection/ProtonVPN.Client.Logic.Connection/RequestCreators/MainSettingsRequestCreator.cs` and `MainSettingsRequestCreatorTest.cs` | The fork's app-port-forwarding flag is a persistent user preference. v5.1.8 does not populate that fork-specific field in Guest Hole settings, even though Guest Hole must keep actual port forwarding disabled. | Keep the release defaults and set only `PortForwardingForApps` from the stored preference; leave `PortForwarding` disabled. Existing true/false preference regressions pass as part of the full connection test project (67/67). |
| `src/Tests/ProtonVPN.Vpn.Tests/Connection/VpnEndpointCandidatesTest.cs` | v5.1.8 covers relay-only Guest Hole hosts, normal hosts, exhaustion, and duplicate IPs. The fork adds a cross-protocol regression where previously tried relay/entry IPs overlap across hosts. | Keep both suites; they cover separate candidate-selection edge cases and the merged implementation uses the same tried-IP tracking. The focused VPN suite passed (12/12). |
| `src/ProcessCommunication/ProtonVPN.ProcessCommunication.EntityMapping.Tests/Vpn/VpnCredentialsMapperTest.cs` | v5.1.8 moved certificate normalization into `ConnectionCertificateMapper` (`88ddeb12a`). Fork commit `d277de99b` updates the credentials test to assert mapper delegation and the null-mapping fallback, with sanitization covered by `ConnectionCertificateMapperTest`. | Keep the architecture-aligned fork regression rather than the stale setup that maps any certificate to one fixture; retain the target mapper sanitization tests. The combined mapper suite passed (12/12). |
| `src/ProtonVPN.Launcher/ProtonVPN.Launcher.csproj` | v5.1.8 enables nullable annotations and links `AppInstanceConstants.cs`, used by the launcher to detect/reopen an existing client. Fork commits `6236327ed`/`ee20c54cf` add the .NET 8 runtime-patch policy; the current fork still references `AppInstanceConstants` in `Program.cs` despite removing that project link. | Keep `<Nullable>enable</Nullable>`, add `<TargetLatestRuntimePatch>true</TargetLatestRuntimePatch>`, and retain the compile link required by `Program.cs`. Launcher build passed with 0 warnings and 0 errors. |

## Remaining validation

No conflict resolutions remain open. The focused suites listed above passed; full release CI and end-to-end validation remain outstanding.
